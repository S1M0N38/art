{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "enabledPlugins": {
    "product-management@knowledge-work-plugins": true,
    "frontend-design@claude-plugins-official": true,
    "claude-md-management@claude-plugins-official": true,
    "claude-code-setup@claude-plugins-official": true,
    "playwright@claude-plugins-official": true,
    "ralph-loop@claude-plugins-official": true
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -qiE '\"file_path\".*\\.(jpg|jpeg|png|webp|avif|gif|svg)'; then echo 'BLOCK: Do not write image files — images belong on BunnyCDN, not in the repo.' >&2; exit 2; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -q 'paintings.yaml'; then node -e \"const yaml=require('yaml'); const fs=require('fs'); try { yaml.parse(fs.readFileSync('src/content/paintings.yaml','utf8')); console.log('YAML valid'); } catch(e) { console.error('YAML ERROR:', e.message); exit(1); }\"; fi"
          }
        ]
      }
    ]
  }
}
